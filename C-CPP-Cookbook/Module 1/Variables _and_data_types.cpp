


    // most important data types

    int number =-5,-4,-3,0,1,2,3,4...n;
    float number = -5.2,7.2,1.5,0.5...n;
    double number = -5.55555,7.5555,8888888 .... n;
    bool logic = true,false,1,0; // 1 = true, 0 = false
    char character = 'a','c','g','3','7';
    string phrase = "This is a string";

// operator = assigns a value into a variable
// Note that we have to declare the datatype of a variable at the beginning
// There are rules for the "identifier" of a variable, which is the name of a variable

/* 
1)  The first character of an identifier has to be
    always a letter

2)  An identifier can not contain blank spaces
     or other character other than those cited.
    Special characters such as *,: /. they are not valid

3)  C++ has case sensitive (lowercase and uppercase)

4)  Can not be a keyword
  

##########################################################################
Examples 
age -> correct
Color camisa -> incorrect (blank space)
67 -> incorrecto (Must be a letter)
max_valor -> correct
minimo% -> incorrect (invalid character %)
Edad_promedio -> correcto
Ca45_uv -> correct
diaFecha -> correct
_salario -> correct

##########################################################################
EXTRA 1:
 if we need to set a variable name which has more than one word, let join them, and the other word begins with uppercase

 Example:

 int maxNumber = 100;



##########################################################################
EXTRA 2:
    For more details, see the picture "data types"
*/
